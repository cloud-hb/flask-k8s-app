# name: CI/CD – Flask k8s App (EKS)

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#     paths:
#       - 'app/**'
#       - 'Dockerfile'
#       - '!docs/**'
#       - '!venv/**'
#       - '!scripts/**'
#       - "!README.md"

# env:
#   IMAGE_NAME: flask-k8s-app
#   ECR_REPOSITORY: flask-k8s-app
#   PRODUCTION_URL: https://your-app.example.com  # # <-- set via env or variable

#   # AWS / EKS (preferably set as repository variables or secrets)
#   AWS_REGION: us-east-1                  # or use ${{ vars.AWS_REGION }}
#   EKS_CLUSTER_NAME: flask-eks-cluster    # or use ${{ vars.EKS_CLUSTER_NAME }}


# permissions:
#   contents: read
#   id-token: write # required for OIDC to AWS

# jobs:
#   # -----------------
#   # CI: lint + tests
#   # -----------------
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v7.0.1

#       - name: Set up Python
#         uses: actions/setup-python@v7.0.0
#         with:
#           python-version: "3.12"
#           cache: "pip"

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           # If you have test deps:
#           # pip install -r requirements-test.txt

#       - name: Run tests
#         run: |
#           # Adjust to your test command, e.g. pytest
#           python -m pytest tests/ || echo "No tests found or tests skipped"

#   # -------------------------
#   # Build & push Docker image
#   # -------------------------
#   build:
#     runs-on: ubuntu-latest
#     needs: test
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'
#     permissions:
#       contents: read
#       id-token: write   # for OIDC to AWS

#     outputs:
#       image_tag: ${{ steps.meta.outputs.version }}
#       image_uri: ${{ steps.ecr-login.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ steps.meta.outputs.version }}

#     env:
#       # AWS / EKS (preferably set as repository variables or secrets)
#       AWS_REGION: us-east-1                  # or use ${{ vars.AWS_REGION }}
#       EKS_CLUSTER_NAME: flask-eks-cluster    # or use ${{ vars.EKS_CLUSTER_NAME }}

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v7.0.1
    
#         # 1. Authenticate to AWS (OIDC)
#       - name: Configure AWS credentials via OIDC
#         uses: aws-actions/configure-aws-credentials@v6.2.3
#         with:
#           role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#         # 2. Log in to Amazon ECR
#       - name: Login to Amazon ECR
#         id: ecr-login
#         uses: aws-actions/amazon-ecr-login@v2.1.6
#         with:
#           registry-type: PRIVATE
#         # run: |
#         #   REGISTRY=$(aws ecr get-login-password --region ${{ env.AWS_REGION }} \
#         #     | docker login --username AWS --password-stdin ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com >/dev/null 2>&1 \
#         #     && echo "${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com")
#         #   echo "registry=$REGISTRY" >> $GITHUB_OUTPUT

#       # 3. Extract Docker metadata (tags, labels)
#       - name: Extract Docker metadata
#         id: meta
#         uses: docker/metadata-action@v6.2.0
#         with:
#           images: ${{ secrets.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}
#           tags: |
#             type=sha,prefix=
#             type=ref,event=branch

#       # 4. Build and push Docker image
#       - name: Build and push Docker image
#         uses: docker/build-push-action@v7.3.0
#         with:
#           context: .
#           file: Dockerfile
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}

#   # -------------------------
#   # Deploy to Kubernetes
#   # -------------------------
#   deploy:
#     runs-on: ubuntu-latest
#     needs: build
#     if: github.ref == 'refs/heads/main'
#     environment:
#       name: production
#       url: ${{ env.PRODUCTION_URL }}  # <-- use the variable here

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v7.0.1

#       - name: Set up kubectl
#         uses: azure/setup-kubectl@v5.1.0
#         with:
#           version: "v1.30.0"

#       # 1. Authenticate to AWS (OIDC)
#       - name: Configure AWS credentials via OIDC
#         uses: aws-actions/configure-aws-credentials@v6.2.3
#         with:
#           role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#       # 2. Update kubeconfig for EKS
#       - name: Update kubeconfig for EKS
#         run: |
#           aws eks update-kubeconfig \
#             --name ${{ env.EKS_CLUSTER_NAME }} \
#             --region ${{ env.AWS_REGION }}

#       # 3. Update deployment image to the newly built ECR image
#       - name: Update deployment image tag
#         run: |
#           IMAGE_URI="${{ needs.build.outputs.image_uri }}"
#           echo "Using image: $IMAGE_URI"

#           # Update the image in deployment.yaml
#           # Adjust the deployment/container name if different
#           kubectl set image \
#             deployment/flask-k8s-app \
#             flask-k8s-app="$IMAGE_URI" \
#             --record

#       # 4. Apply Kubernetes manifests (optional if you rely only on kubectl set image)
#       - name: Apply Kubernetes manifests
#         run: |
#           kubectl apply -f deploy/deployment.yaml
#           kubectl apply -f deploy/service.yaml

#       # 5. Wait for rollout to complete
#       - name: Wait for rollout
#         run: |
#           kubectl rollout status deployment/flask-k8s-app --timeout=300s